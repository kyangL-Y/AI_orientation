# 变更提案: backend-package-build

## 元信息
```yaml
类型: 交付/验证
方案类型: implementation
优先级: P2
状态: 已确认
创建: 2026-03-30
```

---

## 1. 需求

### 背景
用户要求执行“打包编译”。当前上下文未额外指定目标，按确认阶段约定默认针对 `training-agent-master` 后端 Maven 工程执行打包，并核验产物是否成功更新。

### 目标
- 执行 `training-agent-master` 后端 Maven 打包。
- 确认 `ruoyi-admin/target` 生成新的可交付 jar 产物。
- 记录本次打包验证结果，便于后续追踪。

### 约束条件
```yaml
时间约束: 单轮完成，不扩展到管理端或用户端前端构建
性能约束: 使用现有本地依赖缓存，不引入额外环境改造
兼容性约束: 不修改业务代码，仅执行构建命令与结果核验
业务约束: 默认采用后端根工程打包，产物以 ruoyi-admin/target 为准
```

### 验收标准
- [ ] Maven 打包命令成功返回
- [ ] `ruoyi-admin/target/ruoyi-admin.jar` 更新时间刷新为本次执行时间附近
- [ ] 执行结果已同步到知识库记录

---

## 2. 方案

### 技术方案
在 `training-agent-master` 根目录执行 Maven 打包命令，使用跳过测试的后端打包流程生成最新 jar。随后检查 `ruoyi-admin/target` 下的 jar 文件时间戳和存在性，确认产物已刷新。整个流程不修改代码，只做构建与验证。

### 影响范围
```yaml
涉及模块:
  - training-agent-master: 根 Maven 聚合工程打包
  - ruoyi-admin: 目标 jar 产物输出目录
预计变更文件: 0（仅构建产物变化）
```

### 风险评估
| 风险 | 等级 | 应对 |
|------|------|------|
| 本地环境依赖缺失导致打包失败 | 中 | 直接返回 Maven 错误信息，保留失败上下文 |
| 误打包错误目标 | 低 | 按确认阶段默认约定，仅执行 training-agent-master 根工程 |

---

## 4. 核心场景

> 执行完成后同步到对应模块文档

### 场景: 后端打包交付
**模块**: `training-agent-master`
**条件**: Maven 环境可用，工程依赖已可解析
**行为**: 在根目录执行后端打包命令并检查 `ruoyi-admin/target` 产物
**结果**: 生成新的 `ruoyi-admin.jar`，可用于后续部署或运行验证

---

## 5. 技术决策

> 本方案涉及的技术决策，归档后成为决策的唯一完整记录

### backend-package-build#D001: 默认只执行后端根工程 Maven 打包
**日期**: 2026-03-30
**状态**: ✅采纳
**背景**: “打包编译”未明确指定目标，但当前上下文紧接后端任务，确认阶段已声明默认目标为 `training-agent-master` 后端。
**选项分析**:
| 选项 | 优点 | 缺点 |
|------|------|------|
| A: 打包 `training-agent-master` 后端根工程 | 与当前上下文一致，产物明确 | 不覆盖两个前端项目 |
| B: 同时打包后端和两个前端 | 覆盖更全 | 超出当前默认约定，执行成本更高 |
**决策**: 选择方案 A
**理由**: 在用户未补充目标的前提下，优先执行确认阶段已约定的默认目标，避免误扩大范围。
**影响**: 仅影响 `training-agent-master` 后端构建流程与知识库记录
